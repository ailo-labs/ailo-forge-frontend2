/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // Use the "app" directory without deprecated experimental options.
  experimental: {}
};

module.exports = nextConfig;
